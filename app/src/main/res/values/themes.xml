<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.CoursesApp" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/black_background</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="colorPrimaryVariant">@color/black_background</item>
        <item name="colorOutline">@color/black_background</item>
    </style>
</resources>